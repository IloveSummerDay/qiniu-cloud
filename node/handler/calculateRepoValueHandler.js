/**
 * @param {Number} repo_importance - 项目重要程度
 * @param {Number} repo_contribution - 开发者对项目的贡献度
 * @return {Number} 
 */
export const calculate_repo_value = (repo_importance, repo_contribution) => {
    const repo_value = 0;
    return repo_value
}